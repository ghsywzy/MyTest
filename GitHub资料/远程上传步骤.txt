1.验证是否成功，在git bash命令中输入
ssh -T git@github.com
2.接下来我们做的就是把本地仓库传到github上去，
在此之前还需要设置username和email
因为github每次commit都会记录他们

1：加载修改的文件：git add .

2：提交注释: git commit  -m "annotation"

3：提交：git push  (origin master)括号可以不要